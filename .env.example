# Copy this file to `.env` and fill in secrets. `.env` is git-ignored.
# These are only needed if you enable the matching notification channel.

# SMTP password for the email channel (e.g. a Gmail "app password").
HOME_AUTO_EMAIL_PASSWORD=

# Optional: override the config file location (defaults to ./config.yaml).
# HOME_AUTO_CONFIG=/path/to/config.yaml
