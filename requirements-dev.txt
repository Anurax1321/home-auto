# Development + test dependencies. Install with:
#   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0               # test runner
pytest-asyncio>=0.23      # lets us write `async def test_...` coroutine tests
respx>=0.21               # mocks httpx requests so tests never hit the real network
