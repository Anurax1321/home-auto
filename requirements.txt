# Runtime dependencies for the home-auto service (Phase 1).
# Pinned to compatible minimum versions; bump deliberately.

httpx>=0.27,<1.0          # async HTTP client used for ComEd + weather APIs
fastapi>=0.110,<1.0       # web framework powering the local dashboard + JSON API
uvicorn[standard]>=0.29,<1.0  # ASGI server that runs the FastAPI app
apscheduler>=3.10,<4.0    # background scheduler that polls prices on an interval
pydantic>=2.6,<3.0        # data models + validation for config and API responses
PyYAML>=6.0               # parses config.yaml
python-dotenv>=1.0        # loads the .env file for secrets
jinja2>=3.1               # renders the dashboard HTML template
